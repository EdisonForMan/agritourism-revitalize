import Vue from 'vue'
import Vuex from 'vuex'
import { bottomArray } from "@/Mock/bottom";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    bottomArray
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
